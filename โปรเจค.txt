-- [[ CONFIG & THEME ]] --
local guiColor1 = Color3.fromRGB(0, 0, 0)      -- Black
local guiColor2 = Color3.fromRGB(255, 0, 0)    -- Neon Red

-- [[ SERVICES ]] --
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local Mouse = LocalPlayer:GetMouse()

-- [[ GLOBAL STATES ]] --
local states = {
    autoPrompt = false,
    espEnabled = false,
    bouncingHead = false,
    espLine = false,      
    espName = false,      
    espDisplayName = false,
    autoFarm = false,
    autoSaveGlock = false 
}
local farmCooldowns = {} 
local TELEPORT_COORD = Vector3.new(-260, 14, 1164)
local FARM_CENTER = Vector3.new(544, 13, -865)

-- [[ UTILITIES ]] --
local function GetBouncingColor()
    return Color3.fromRGB(0, 0, 0):Lerp(Color3.fromRGB(255, 0, 0), (math.sin(tick() * 2) + 1) / 2)
end

local function SmartPressE(prompt)
    if not prompt or not prompt.Enabled then return end
    task.spawn(function()
        prompt:InputHoldBegin()
        task.wait((prompt.HoldDuration or 0) + 0.1)
        prompt:InputHoldEnd()
        fireproximityprompt(prompt)
    end)
end

local function FlyTo(targetCFrame, speed)
    local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    local distance = (hrp.Position - targetCFrame.Position).Magnitude
    local info = TweenInfo.new(distance / speed, Enum.EasingStyle.Linear)
    local tween = TweenService:Create(hrp, info, {CFrame = targetCFrame})
    tween:Play()
    tween.Completed:Wait()
end

-- [[ BYTENET SYSTEM (GLOCK19) ]] --
local function SaveGlock()
    pcall(function()
        local args = { buffer.fromstring("\003\006\000Change\a\000Glock19\b\000Backpack") }
        ReplicatedStorage:WaitForChild("Package"):WaitForChild("ByteNetMax"):WaitForChild("system"):WaitForChild("ByteNetReliable"):FireServer(unpack(args))
    end)
end

local function LoadGlock()
    pcall(function()
        task.wait(1.2)
        local args = { buffer.fromstring("\003\006\000Change\a\000Glock19\003\000Inv") }
        ReplicatedStorage:WaitForChild("Package"):WaitForChild("ByteNetMax"):WaitForChild("system"):WaitForChild("ByteNetReliable"):FireServer(unpack(args))
    end)
end

local function MonitorLifeCycle(character)
    local humanoid = character:WaitForChild("Humanoid")
    humanoid.Died:Connect(function()
        if states.autoSaveGlock then
            SaveGlock() 
            LocalPlayer.CharacterAdded:Wait() 
            LoadGlock() 
        end
    end)
end

if LocalPlayer.Character then task.spawn(MonitorLifeCycle, LocalPlayer.Character) end
LocalPlayer.CharacterAdded:Connect(function(char) task.spawn(MonitorLifeCycle, char) end)

-- [[ AUTO FARM SYSTEM ]] --
task.spawn(function()
    while true do
        if states.autoFarm then
            pcall(function()
                local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
                if hrp then
                    FlyTo(CFrame.new(FARM_CENTER), 100)
                    local farmFolder = workspace:FindFirstChild("Map") and workspace.Map:FindFirstChild("จุดฟาร์ม")
                    if farmFolder then
                        local targets = {}
                        local validNames = {["MachineBase"]=true, ["Microwave"]=true, ["Fridge"]=true}
                        for _, v in pairs(farmFolder:GetChildren()) do
                            if validNames[v.Name] then
                                local dynamicCooldown = math.random(20, 25)
                                if not farmCooldowns[v] or (tick() - farmCooldowns[v]) > dynamicCooldown then
                                    local pos = v:GetPivot().Position
                                    if (pos - Vector3.new(562, 13, -865)).Magnitude < 35 or (pos - Vector3.new(549, 13, -831)).Magnitude < 35 then 
                                        table.insert(targets, v) 
                                    end
                                end
                            end
                        end
                        if #targets > 0 then
                            local selected = targets[math.random(1, #targets)]
                            farmCooldowns[selected] = tick()
                            FlyTo(selected:GetPivot() * CFrame.new(0, 2, 4), 50)
                            local prompt = selected:FindFirstChildWhichIsA("ProximityPrompt", true)
                            if prompt then task.wait(0.2); SmartPressE(prompt); task.wait((prompt.HoldDuration or 0) + 0.6) end
                        end
                    end
                end
            end)
        end
        task.wait(0.5)
    end
end)

-- [[ ESP SYSTEM ]] --
local activeTracers, activeLabels = {}, {}
local function RemoveDrawingESP(player)
    if activeTracers[player] then activeTracers[player]:Remove(); activeTracers[player] = nil end
    if activeLabels[player] then activeLabels[player]:Remove(); activeLabels[player] = nil end
end
local function CreateDrawingESP(player)
    RemoveDrawingESP(player)
    local line = Drawing.new("Line"); line.Visible = false; line.Color = Color3.fromRGB(255, 0, 0); line.Thickness = 1; activeTracers[player] = line
    local text = Drawing.new("Text"); text.Visible = false; text.Color = Color3.fromRGB(255, 255, 255); text.Size = 14; text.Center = true; text.Outline = true; activeLabels[player] = text
end

-- [[ GUI BUILDER V2.3 ]] --
local function buildGUI()
    local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()
    local W = WindUI:CreateWindow({ Title = "YNW HUB [V2.3]", Icon = "skull", Author = "by YNW Melly" })
    W:EditOpenButton({ Title = "YNW HUB", Icon = "ghost", Color = ColorSequence.new(guiColor1, guiColor2), Draggable = true })
    W:Tag({ Title = "V2.3 FINAL", Icon = "crown", Color = Color3.fromHex("#ffcc00"), Radius = 13 })

    -- TAB: FARM
    local MainTab = W:Tab({ Title = "ฟาร์มทั่วไป", Icon = "pickaxe" })
    MainTab:Section({ Title = "ระบบช่วยฟาร์ม" })
    MainTab:Toggle({ Title = "ออโต้ฟาร์ม", Desc = "บินไปฟาร์มให้เอง", Value = false, Callback = function(v) states.autoFarm = v end })
    MainTab:Toggle({ Title = "ออโต้กด E", Desc = "ขี้เกียจกดเองก็เปิดไว้ เดินไปไหนมันกดให้หมดอ่ะมึง", Value = false, Callback = function(v) states.autoPrompt = v end })
    MainTab:Button({
        Title = "ไม่รู้",
        Desc = "อันนี้ชั่งมัน",
        Icon = "mouse-pointer-2",
        Callback = function()
            local tool = Instance.new("Tool", LocalPlayer.Backpack); tool.Name = "YNW | วาร์ปไอเทม"; tool.RequiresHandle = false
            tool.Activated:Connect(function()
                local t = Mouse.Target 
                local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
                if hrp and t and t:IsA("BasePart") then
                    t.CFrame = CFrame.new(TELEPORT_COORD)
                    task.wait(0.1); hrp.CFrame = CFrame.new(TELEPORT_COORD + Vector3.new(0, 5, 0))
                end
            end)
        end
    })

    -- TAB: COMBAT
    local CombatTab = W:Tab({ Title = "สายบวก", Icon = "swords" })
    CombatTab:Section({ Title = "ระบบโจมตี" })
    CombatTab:Toggle({ Title = "โหมดหัวโต", Desc = "ขยายหัวพวกมันให้มึงยิงง่ายๆ หลับตายังโดน", Value = false, Callback = function(v) states.bouncingHead = v end })
    CombatTab:Section({ Title = "ระบบขโมย" })
    CombatTab:Button({ Title = "แสกนไอเทมทั้งเซิร์ฟ", Desc = "ดูดิพวกมันมีไรดี แสกนเสร็จละดึงมาเป็นของมึงซะ", Icon = "scan-eye", Callback = function()
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= LocalPlayer then
                local function check(parent)
                    for _, t in pairs(parent:GetChildren()) do
                        if t:IsA("Tool") and t.Name ~= "Fist" then
                            CombatTab:Button({ Title = "ขโมย: "..t.Name, Desc = "ไอเทมจาก "..p.DisplayName.." ดึงมาดิรอไร", Callback = function() t.Parent = LocalPlayer.Backpack end })
                        end
                    end
                end
                if p:FindFirstChild("Backpack") then check(p.Backpack) end
                if p.Character then check(p.Character) end
            end
        end
    end })

    -- TAB: INVENTORY
    local InvTab = W:Tab({ Title = "จัดการตู้", Icon = "package" })
    InvTab:Section({ Title = "ระบบป้องกันปืนหาย (ถาวร)" })
    InvTab:Button({
        Title = "เก็บปืนเข้าตู้ (Glock19)",
        Desc = "กดแล้วมันจะเอาปืนGlock19เข้าตู้ให้มึง",
        Icon = "archive",
        Callback = function()
            SaveGlock()
            WindUI:Notify({ Title = "YNW HUB", Content = "เก็บปืนเข้าตู้ให้แล้วมึง สบายใจได้!", Duration = 2 })
        end
    })
    InvTab:Toggle({
        Title = "เก็บปืนออโต้ตอนตาย (Glock19)",
        Desc = "กันปืนตกกากๆ",
        Value = false,
        Callback = function(v) states.autoSaveGlock = v end
    })

    -- TAB: VISUAL
    local VisualTab = W:Tab({ Title = "สายตา", Icon = "eye" })
    VisualTab:Section({ Title = "ระบบมองทะลุ (ESP)" })
    VisualTab:Toggle({ Title = "แสดงเส้น Tracer", Desc = "ลากเส้นไปหาคนอื่นจะได้รู้ว่าอยู่ไหนกันบ้าง", Callback = function(v) states.espLine = v end })
    VisualTab:Toggle({ Title = "แสดงชื่อจริง", Desc = "โชว์ชื่อแม่งบนหัว", Callback = function(v) states.espName = v; if v then states.espDisplayName = false end end })
    VisualTab:Toggle({ Title = "แสดงชื่อเล่น", Desc = "ชื่อเล่น", Callback = function(v) states.espDisplayName = v; if v then states.espName = false end end })

    -- TAB: GUN MODS
    local GunTab = W:Tab({ Title = "แต่งปืน", Icon = "target" })
    
    local function ApplyGunMod(mode)
        local tool = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Tool")
        if tool and tool:FindFirstChild("Setting") then
            local gun = require(tool.Setting)
            
            -- Reset Effects
            gun.ExplosiveEnabled = false
            gun.FreezingBullet = false
            gun.FlamingBullet = false
            
            if mode == "Explosive" then
                gun.LimitedAmmoEnabled = false; gun.Ammo = 999; gun.ReloadTime = 0; gun.Auto = true; gun.Recoil = 0
                gun.ExplosiveEnabled = true
            elseif mode == "Freeze" then
                gun.LimitedAmmoEnabled = false; gun.Ammo = 999; gun.ReloadTime = 0; gun.Auto = true; gun.Recoil = 0
                gun.FreezingBullet = true
            elseif mode == "Fire" then
                gun.LimitedAmmoEnabled = false; gun.Ammo = 999; gun.ReloadTime = 0; gun.Auto = true; gun.Recoil = 0
                gun.FlamingBullet = true
            elseif mode == "FireFreeze" then
                gun.LimitedAmmoEnabled = false; gun.Ammo = 999; gun.ReloadTime = 0; gun.Auto = true; gun.Recoil = 0
                gun.FlamingBullet = true; gun.FreezingBullet = true
            elseif mode == "NormalCustom" then
                gun.MaxAmmo = math.huge; gun.Ammo = math.huge; gun.AmmoPerMag = math.huge
                gun.LimitedAmmoEnabled = false; gun.Auto = true; gun.Spread = 0.1
                gun.MaximumTime = 0.001; gun.ChargingTime = 0.001; gun.Recoil = 0.001
                gun.ExplosiveEnabled = true; gun.CooldownRate = 0.001; gun.SelfDamage = false
                gun.ReloadTime = 0.001; gun.RecoilSpeed = 0.001; gun.FriendlyFire = true
                gun.LifeSteal = 100; gun.CanSpinPart = true; gun.HoldingTime = 0.001
                gun.BulletPerBurst = 30; gun.BurstRate = 30; gun.MaxHeat = 99999999
            elseif mode == "FullCustom" then
                gun.MaxAmmo = math.huge; gun.Ammo = math.huge; gun.AmmoPerMag = math.huge
                gun.LimitedAmmoEnabled = false; gun.Auto = true; gun.Spread = 0.1
                gun.MaximumTime = 0.001; gun.ChargingTime = 0.001; gun.Recoil = 0.001
                gun.CooldownRate = 0.001; gun.SelfDamage = false; gun.ReloadTime = 0.001
                gun.RecoilSpeed = 0.001; gun.FriendlyFire = true; gun.LifeSteal = 10000
                gun.CanSpinPart = true; gun.HoldingTime = 0.001; gun.BulletPerBurst = 30
                gun.BurstRate = 30; gun.MaxHeat = 99999999
            elseif mode == "NoOp" then
                gun.LimitedAmmoEnabled = false; gun.Ammo = 999; gun.ReloadTime = 0; gun.Auto = true; gun.Recoil = 0
            end
            WindUI:Notify({ Title = "YNW HUB", Content = "ปรับแต่งปืนโหมด " .. mode .. " เรียบร้อย!", Duration = 2 })
        end
    end

    GunTab:Section({ Title = "ตัวเลือกการปรับแต่ง" })
    GunTab:Button({ Title = "โหมดระเบิด", Desc = "ยิงระเบิดตู้มต้าม", Callback = function() ApplyGunMod("Explosive") end })
    GunTab:Button({ Title = "โหมดแช่แข็ง", Desc = "ยิงแล้วแข็ง", Callback = function() ApplyGunMod("Freeze") end })
    GunTab:Button({ Title = "โหมดไฟลุก", Desc = "ยิงและไฟลุก", Callback = function() ApplyGunMod("Fire") end })
    GunTab:Button({ Title = "โหมดไฟ+น้ำแข็ง", Desc = "ยิงแล้วแข็ง+ไฟลุก", Callback = function() ApplyGunMod("FireFreeze") end })
    GunTab:Button({ Title = "กระสุนไม่จำกัดแบบระเบิดกากหน่อย", Desc = "ยิงรัวๆ ระเบิดตู้มต้ามแบบสเตตัสเป๊ะๆ", Callback = function() ApplyGunMod("NormalCustom") end })
    GunTab:Button({ Title = "ปรับกระสุนไม่จำกัด", Desc = "กระสุนไม่จำกัด", Callback = function() ApplyGunMod("FullCustom") end })
    GunTab:Button({ Title = "แบบพอดี (No OP)", Desc = "ปรับนิดๆ หน่อยๆ ให้พอมีสีสัน", Callback = function() ApplyGunMod("NoOp") end })
end

-- [[ SYSTEM LOOPS ]] --
RunService.RenderStepped:Connect(function()
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Head") then
            local head = player.Character.Head
            local vector, onScreen = Camera:WorldToViewportPoint(head.Position)
            if activeTracers[player] then
                activeTracers[player].Visible = states.espLine and onScreen
                if activeTracers[player].Visible then activeTracers[player].From = Vector2.new(Camera.ViewportSize.X/2, 0); activeTracers[player].To = Vector2.new(vector.X, vector.Y) end
            end
            if activeLabels[player] then
                activeLabels[player].Visible = (states.espName or states.espDisplayName) and onScreen
                if activeLabels[player].Visible then activeLabels[player].Position = Vector2.new(vector.X, vector.Y - 25); activeLabels[player].Text = states.espDisplayName and player.DisplayName or player.Name end
            end
        end
    end
end)

RunService.Heartbeat:Connect(function()
    if states.autoPrompt and not states.autoFarm then
        for _, v in pairs(workspace:GetDescendants()) do if v:IsA("ProximityPrompt") and v.Enabled then fireproximityprompt(v) end end
    end
    if states.bouncingHead then
        local color = GetBouncingColor()
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("Head") then
                p.Character.Head.Size, p.Character.Head.Color, p.Character.Head.CanCollide = Vector3.new(5,5,5), color, false
            end
        end
    end
end)

for _, p in pairs(Players:GetPlayers()) do if p ~= LocalPlayer then CreateDrawingESP(p) end end
Players.PlayerAdded:Connect(function(p) p.CharacterAdded:Connect(function() CreateDrawingESP(p) end) end)
Players.PlayerRemoving:Connect(RemoveDrawingESP)

buildGUI()
WindUI:Notify({ Title = "YNW HUB V2.3", Content = "โหลดข้อมูลครบพร้อมลุยไปซ่าได้เลย!", Duration = 5 })
