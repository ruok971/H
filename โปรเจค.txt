-- [[ CONFIG & THEME ]] --
local guiColor1 = Color3.fromRGB(0, 0, 0)      -- Black
local guiColor2 = Color3.fromRGB(255, 0, 0)    -- Neon Red
local windUIGuis = {}

-- [[ SERVICES ]] --
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local Mouse = LocalPlayer:GetMouse()

-- [[ GLOBAL STATES ]] --
local states = {
    autoPrompt = false,
    espEnabled = false,
    bouncingHead = false,
    espLine = false,      
    espName = false,      
    espDisplayName = false,
    autoFarm = false
}
local farmCooldowns = {} 
local selectedItemToStore = ""
local TELEPORT_COORD = Vector3.new(-260, 14, 1164)
local FARM_CENTER = Vector3.new(544, 13, -865)

-- [[ UTILITIES ]] --
local function GetBouncingColor()
    return Color3.fromRGB(0, 0, 0):Lerp(Color3.fromRGB(255, 0, 0), (math.sin(tick() * 2) + 1) / 2)
end

local function SmartPressE(prompt)
    if not prompt or not prompt.Enabled then return end
    task.spawn(function()
        prompt:InputHoldBegin()
        local duration = prompt.HoldDuration or 0
        task.wait(duration + 0.1)
        prompt:InputHoldEnd()
        fireproximityprompt(prompt)
    end)
end

local function FlyTo(targetCFrame, speed)
    local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    local distance = (hrp.Position - targetCFrame.Position).Magnitude
    local duration = distance / speed
    local info = TweenInfo.new(duration, Enum.EasingStyle.Linear)
    local tween = TweenService:Create(hrp, info, {CFrame = targetCFrame})
    tween:Play()
    tween.Completed:Wait()
end

-- [[ AUTO FARM SYSTEM ]] --
task.spawn(function()
    while true do
        if states.autoFarm then
            pcall(function()
                local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
                if hrp then
                    FlyTo(CFrame.new(FARM_CENTER), 100)
                    task.wait(0.3)
                    local farmFolder = workspace:FindFirstChild("Map") and workspace.Map:FindFirstChild("จุดฟาร์ม")
                    if farmFolder then
                        local targets = {}
                        local validNames = {["MachineBase"]=true, ["Microwave"]=true, ["Fridge"]=true}
                        for _, v in pairs(farmFolder:GetChildren()) do
                            if validNames[v.Name] then
                                local dynamicCooldown = math.random(20, 25)
                                if not farmCooldowns[v] or (tick() - farmCooldowns[v]) > dynamicCooldown then
                                    local pos = v:GetPivot().Position
                                    local inZone1 = (pos - Vector3.new(562, 13, -865)).Magnitude < 35
                                    local inZone2 = (pos - Vector3.new(549, 13, -831)).Magnitude < 35
                                    if inZone1 or inZone2 then table.insert(targets, v) end
                                end
                            end
                        end
                        if #targets > 0 then
                            local selected = targets[math.random(1, #targets)]
                            farmCooldowns[selected] = tick()
                            local targetPivot = selected:GetPivot()
                            local offsetCFrame = targetPivot * CFrame.new(0, 2, 4) -- ลอยหน้าไอเทม 4 หน่วย
                            FlyTo(offsetCFrame, 50)
                            local prompt = selected:FindFirstChildWhichIsA("ProximityPrompt", true)
                            if prompt then
                                task.wait(0.2)
                                SmartPressE(prompt)
                                task.wait((prompt.HoldDuration or 0) + 0.6)
                            end
                        end
                    end
                end
            end)
        end
        task.wait(0.5)
    end
end)

-- [[ ESP SYSTEM ]] --
local activeTracers = {}
local activeLabels = {}
local function RemoveDrawingESP(player)
    if activeTracers[player] then activeTracers[player]:Remove(); activeTracers[player] = nil end
    if activeLabels[player] then activeLabels[player]:Remove(); activeLabels[player] = nil end
end
local function CreateDrawingESP(player)
    RemoveDrawingESP(player)
    local line = Drawing.new("Line"); line.Visible = false; line.Color = Color3.fromRGB(255, 0, 0); line.Thickness = 1; activeTracers[player] = line
    local text = Drawing.new("Text"); text.Visible = false; text.Color = Color3.fromRGB(255, 255, 255); text.Size = 14; text.Center = true; text.Outline = true; activeLabels[player] = text
end

RunService.RenderStepped:Connect(function()
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Head") then
            local head = player.Character.Head
            local vector, onScreen = Camera:WorldToViewportPoint(head.Position)
            if activeTracers[player] then
                activeTracers[player].Visible = states.espLine and onScreen
                if activeTracers[player].Visible then
                    activeTracers[player].From = Vector2.new(Camera.ViewportSize.X / 2, 0); activeTracers[player].To = Vector2.new(vector.X, vector.Y)
                end
            end
            if activeLabels[player] then
                activeLabels[player].Visible = (states.espName or states.espDisplayName) and onScreen
                if activeLabels[player].Visible then
                    activeLabels[player].Position = Vector2.new(vector.X, vector.Y - 25)
                    activeLabels[player].Text = states.espDisplayName and player.DisplayName or player.Name
                end
            end
        else
            if activeTracers[player] then activeTracers[player].Visible = false end
            if activeLabels[player] then activeLabels[player].Visible = false end
        end
    end
end)

-- [[ GUI BUILDER ]] --
local function buildGUI()
    local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()
    local W = WindUI:CreateWindow({ Title = "YNW HUB [OP]", Icon = "skull", Author = "by YNW Melly" })
    W:EditOpenButton({ Title = "YNW HUB", Icon = "ghost", Color = ColorSequence.new(guiColor1, guiColor2), Draggable = true })
    W:Tag({ Title = "v1.9 FINAL", Icon = "crown", Color = Color3.fromHex("#ffcc00"), Radius = 13 })

    -- TAB: FARM
    local MainTab = W:Tab({ Title = "ฟาร์มทั่วไป", Icon = "pickaxe" })
    MainTab:Section({ Title = "ระบบช่วยฟาร์ม" })
    MainTab:Toggle({
        Title = "ออโต้ฟาร์ม",
        Desc = "บินฟาร์ม",
        Value = false,
        Callback = function(v) states.autoFarm = v end
    })
    MainTab:Toggle({
        Title = "ออโต้กด E",
        Desc = "กดปุ่ม ProximityPrompt ทุกอย่างที่อยู่ใกล้ตัว",
        Value = false,
        Callback = function(v) states.autoPrompt = v end
    })
    MainTab:Button({
        Title = "วาร์ป",
        Desc = "ชั่งแม่ง",
        Icon = "mouse-pointer-2",
        Callback = function()
            local tool = Instance.new("Tool", LocalPlayer.Backpack); tool.Name = "YNW | วาร์ปไอเทม"; tool.RequiresHandle = false
            tool.Activated:Connect(function()
                local t = Mouse.Target 
                local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
                if hrp and t and t:IsA("BasePart") then
                    t.CFrame = CFrame.new(TELEPORT_COORD)
                    task.wait(0.1)
                    hrp.CFrame = CFrame.new(TELEPORT_COORD + Vector3.new(0, 5, 0))
                end
            end)
        end
    })

    -- TAB: COMBAT
    local CombatTab = W:Tab({ Title = "สายบวก", Icon = "swords" })
    CombatTab:Section({ Title = "ระบบโจมตี" })
    CombatTab:Toggle({
        Title = "โหมดหัวโต",
        Desc = "ขยายขนาดหัวศัตรูให้ใหญ่ขึ้นและเปลี่ยนสีเพื่อการยิงที่แม่นยำ",
        Value = false,
        Callback = function(v) states.bouncingHead = v end
    })
    CombatTab:Section({ Title = "ระบบขโมย" })
    CombatTab:Button({
        Title = "แสกนไอเทมทั้งเซิร์ฟ",
        Desc = "แสดงปุ่มเพื่อดึงไอเทมคนอื่นมาที่ตัวเรา",
        Icon = "scan-eye",
        Callback = function()
            for _, p in pairs(Players:GetPlayers()) do
                if p ~= LocalPlayer then
                    local function checkTools(parent)
                        for _, t in pairs(parent:GetChildren()) do
                            if t:IsA("Tool") and t.Name ~= "Fist" then
                                CombatTab:Button({ Title = "ขโมยจาก: "..p.DisplayName.." ["..t.Name.."]", Desc = "คลิกเพื่อดึงไอเทมชิ้นนี้เข้ากระเป๋า", Callback = function() t.Parent = LocalPlayer.Backpack end })
                            end
                        end
                    end
                    if p:FindFirstChild("Backpack") then checkTools(p.Backpack) end
                    if p.Character then checkTools(p.Character) end
                end
            end
        end
    })

    -- TAB: INVENTORY
    local InvTab = W:Tab({ Title = "จัดการตู้", Icon = "package" })
    InvTab:Section({ Title = "ระบบจัดการไอเทม" })
    InvTab:Input({
        Title = "ระบุชื่อไอเทม",
        Placeholder = "ชื่อไอเทมที่จะเก็บ...",
        Desc = "พิมพ์ชื่อไอเทมในกระเป๋าที่ต้องการส่งเข้าตู้เซฟ",
        Callback = function(v) selectedItemToStore = v end
    })
    InvTab:Button({
        Title = "ส่งไอเทมเข้าตู้",
        Desc = "ส่งไอเทมตามชื่อที่ระบุไว้เข้าตู้เซฟทันที",
        Callback = function() 
            if selectedItemToStore ~= "" then ReplicatedStorage.Inventory:FireServer("Change", selectedItemToStore, "Backpack") end 
        end
    })
    InvTab:Button({
        Title = "เก็บของทั้งหมด",
        Desc = "เอาไอเทมทุกอย่างในกระเป๋าเข้าตู้เซฟทั้งหมด",
        Icon = "archive",
        Callback = function()
            for _, t in pairs(LocalPlayer.Backpack:GetChildren()) do
                if t:IsA("Tool") then ReplicatedStorage.Inventory:FireServer("Change", t.Name, "Backpack") end
            end
        end
    })

    -- TAB: VISUAL
    local VisualTab = W:Tab({ Title = "สายตา", Icon = "eye" })
    VisualTab:Section({ Title = "ระบบมอง" })
    VisualTab:Toggle({ Title = "แสดงเส้น Tracer", Desc = "ลากเส้นจากกลางจอไปหาผู้เล่นคนอื่น", Callback = function(v) states.espLine = v end })
    VisualTab:Toggle({ Title = "แสดงชื่อจริง", Desc = "แสดงชื่อของผู้เล่นบนหัว", Callback = function(v) states.espName = v; if v then states.espDisplayName = false end end })
    VisualTab:Toggle({ Title = "แสดงชื่อเล่น", Desc = "แสดงชื่อเล่นของผู้เล่นบนหัว", Callback = function(v) states.espDisplayName = v; if v then states.espName = false end end })

    -- TAB: GUN MODS
    local GunTab = W:Tab({ Title = "แต่งปืน", Icon = "target" })
    local function ApplyGunMod(mode)
        local tool = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Tool")
        if tool and tool:FindFirstChild("Setting") then
            local gun = require(tool.Setting)
            gun.LimitedAmmoEnabled = false; gun.Ammo = 999; gun.ReloadTime = 0; gun.Auto = true; gun.Recoil = 0
            gun.ExplosiveEnabled = (mode == "Explosive"); gun.FreezingBullet = (mode == "Freeze"); gun.FlamingBullet = (mode == "Fire")
            WindUI:Notify({ Title = "สำเร็จ", Content = "โหมดแต่งปืน: " .. mode, Duration = 2 })
        end
    end
    GunTab:Button({ Title = "โหมดระเบิด", Desc = "เปลี่ยนกระสุนธรรมดาให้เป็นระเบิด (ต้องถือปืน)", Callback = function() ApplyGunMod("Explosive") end })
    GunTab:Button({ Title = "โหมดแช่แข็ง", Desc = "ยิงแล้วศัตรูจะติดสถานะตัวแข็ง (ต้องถือปืน)", Callback = function() ApplyGunMod("Freeze") end })
    GunTab:Button({ Title = "โหมดไฟลุก", Desc = "ยิงแล้วศัตรูจะติดสถานะไฟไหม้ (ต้องถือปืน)", Callback = function() ApplyGunMod("Fire") end })
    GunTab:Button({ Title = "แบบพอดี", Desc = "ปรับแบบไม่opเยอะ (ต้องถือปืน)", Callback = function() ApplyGunMod("NO OP") end })
end

-- [[ SYSTEM LOOPS ]] --
RunService.Heartbeat:Connect(function()
    if states.autoPrompt and not states.autoFarm then
        for _, v in pairs(workspace:GetDescendants()) do
            if v:IsA("ProximityPrompt") and v.Enabled then fireproximityprompt(v) end
        end
    end
    if states.bouncingHead then
        local color = GetBouncingColor()
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("Head") then
                p.Character.Head.Size = Vector3.new(5, 5, 5)
                p.Character.Head.Color = color
                p.Character.Head.CanCollide = false
            end
        end
    end
end)

for _, p in pairs(Players:GetPlayers()) do if p ~= LocalPlayer then CreateDrawingESP(p) end end
Players.PlayerAdded:Connect(function(p) p.CharacterAdded:Connect(function() CreateDrawingESP(p) end) end)
Players.PlayerRemoving:Connect(RemoveDrawingESP)

buildGUI()
WindUI:Notify({ Title = "YNW HUB V.1.5", Content = "โหลดข้อมูลครบพร้อมลุย!", Duration = 5 })
